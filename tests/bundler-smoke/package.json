{
    "name": "bundler-smoke-test",
    "private": true,
    "type": "module",
    "scripts": {
        "build": "vite build",
        "test": "vite build && node verify-build.js"
    },
    "dependencies": {
        "libadlmidi-js": "file:../.."
    },
    "devDependencies": {
        "vite": "^6.3.5"
    }
}